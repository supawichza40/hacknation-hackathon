# --- secrets (never commit; ship .env.example instead) ---
.env
.env.*
!.env.example
*.pem
*.key

# --- python ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# --- node / frontend ---
node_modules/
dist/
build/
.next/
.vite/
coverage/
*.tsbuildinfo

# --- OS / editor junk ---
.DS_Store
Thumbs.db
*.swp

# --- office lock files (deck editing) ---
~$*

# --- sync-conflict duplicates (iCloud/Dropbox pattern — but keep the repo OUT of sync folders) ---
* 2.*
*conflicted copy*

# --- logs & temp ---
*.log
tmp/
*.zip

# --- cloned demo repo mirrors (78M+; only keep derived graph+replay artifacts) ---
data/demo/repos/

# --- generated SQLite DB (regenerated by `npm run seed`) ---
data/vc-brain.db
data/vc-brain.db-shm
data/vc-brain.db-wal

# --- Lovable export (TanStack Start app; separate from the Next.js app) ---
lovable/node_modules/
lovable/.output/
lovable/.vinxi/
lovable/dist/
