export default function ApplyPage() {
  return (
    <section>
      <h1 style={{ fontSize: "var(--text-title)", margin: "0 0 8px" }}>Apply</h1>
      <p style={{ color: "var(--muted)", margin: 0 }}>
        Inbound founder application — company, problem, ask, deck + GitHub. Scaffold placeholder (M0).
      </p>
    </section>
  );
}
