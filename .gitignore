# --- secrets (never commit; ship .env.example instead) ---
.env
.env.*
!.env.example
*.pem
*.key

# --- python ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# --- node / frontend ---
node_modules/
dist/
build/
.next/
.vite/
coverage/
*.tsbuildinfo

# --- OS / editor junk ---
.DS_Store
Thumbs.db
*.swp

# --- office lock files (deck editing) ---
~$*

# --- sync-conflict duplicates (iCloud/Dropbox pattern — but keep the repo OUT of sync folders) ---
* 2.*
*conflicted copy*

# --- logs & temp ---
*.log
tmp/
*.zip
