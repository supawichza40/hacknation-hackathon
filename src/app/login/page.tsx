export default function LoginPage() {
  return (
    <section>
      <h1 style={{ fontSize: "var(--text-title)", margin: "0 0 8px" }}>Login</h1>
      <p style={{ color: "var(--muted)", margin: 0 }}>
        Demo-lite investor login (plan §0.5 d13) — single hard-coded investor session. Scaffold placeholder (M0).
      </p>
    </section>
  );
}
